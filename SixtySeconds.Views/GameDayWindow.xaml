<Window x:Class="SixtySeconds.Views.GameDayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SixtySeconds.Views"
        mc:Ignorable="d"
        Title="GameDayWindow" Width="600" Height="400">
    <Window.Resources>
        <Style x:Key="TextBoxInError" TargetType="Control">
            <Setter Property="Validation.ErrorTemplate">
                <Setter.Value>
                    <ControlTemplate>
                        <DockPanel>
                            <TextBlock Foreground="Red" FontWeight="Bold"></TextBlock>
                            <TextBlock Text="{Binding ErrorContent}" DockPanel.Dock="Bottom" Foreground="Red" TextAlignment="Center" Margin="5 5 0 0"/>
                            <Border BorderBrush="Red" BorderThickness="2">
                                <AdornedElementPlaceholder/>
                            </Border>
                        </DockPanel>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Validation.HasError" Value="True">
                    <Setter
            Property="ToolTip"
            Value="{Binding RelativeSource={x:Static RelativeSource.Self},
            Path=(Validation.Errors)/ErrorContent}" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="TextBlock" TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="Margin" Value="10 10 10 10"/>
                <Setter Property="TextAlignment" Value="Center"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style.Setters>
        </Style>
        <Style x:Key="TextBox" TargetType="TextBox">
            <Style.Setters>
                <Setter Property="Margin" Value="0 5 10 5"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style.Setters>
        </Style>
    </Window.Resources>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Team IDs" Width="100" Style="{StaticResource TextBlock}"/>
                <TextBox Text="{Binding TeamIds, UpdateSourceTrigger=PropertyChanged}" Width="100" Style="{StaticResource TextBox}"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Best teams" Width="75" Style="{StaticResource TextBlock}"/>
                <TextBox Text="{Binding BestTeams, UpdateSourceTrigger=PropertyChanged}" Width="50" Style="{StaticResource TextBox}"/>
            </StackPanel>
            <Button Command="{Binding ShowCharts}" Content="Show charts" Margin="0 5 10 5" Width="150" VerticalAlignment="Center" />
            <TextBlock Foreground="Red"
                       Visibility="{Binding ChartsErrorMessageVisibility}"
                       Text="{Binding ChartsErrorMessage}"
                       Style="{StaticResource TextBlock}"/>
        </StackPanel>
    <Separator/>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Team ID" Width="100" Style="{StaticResource TextBlock}"/>
            <TextBox Text="{Binding TeamID, UpdateSourceTrigger=PropertyChanged}" Width="50" Style="{StaticResource TextBox}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="SpreadsheetID" Width="100" Style="{StaticResource TextBlock}"/>
            <TextBox Text="{Binding SpreadsheetID, UpdateSourceTrigger=PropertyChanged}" Width="200" Style="{StaticResource TextBox}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Sheet" Width="100" Style="{StaticResource TextBlock}"/>
            <TextBox Text="{Binding SheetName, UpdateSourceTrigger=PropertyChanged}" Width="100" Style="{StaticResource TextBox}"/>
        </StackPanel>
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                
                <TextBlock Text="Rows" Style="{StaticResource TextBlock}" Grid.Row="0" Grid.Column="0"/>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="0">
                    <TextBlock Text="First Question" Style="{StaticResource TextBlock}"/>
                    <TextBox Text="{Binding FirstQuestionColumn, UpdateSourceTrigger=PropertyChanged}" Width="100" Style="{StaticResource TextBox}"/>
                </StackPanel>
                
                <TextBlock Text="Columns" Style="{StaticResource TextBlock}" Grid.Row="0" Grid.Column="1"/>
                <StackPanel Orientation="Horizontal" Grid.Row="1" Grid.Column="1">
                    <TextBlock Text="Team answered" Width="100" Style="{StaticResource TextBlock}"/>
                    <TextBox Text="{Binding TeamAnsweredColumn, UpdateSourceTrigger=PropertyChanged}" Width="100" Style="{StaticResource TextBox}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1">
                    <TextBlock Text="Right Answers" Width="100" Style="{StaticResource TextBlock}"/>
                    <TextBox Text="{Binding RightAnswersColumn, UpdateSourceTrigger=PropertyChanged}" Width="100" Style="{StaticResource TextBox}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="1">
                    <TextBlock Text="Places" Width="100" Style="{StaticResource TextBlock}"/>
                    <TextBox Text="{Binding PlacesColumn, UpdateSourceTrigger=PropertyChanged}" Width="100" Style="{StaticResource TextBox}"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Grid.Row="4" Grid.Column="1">
                    <TextBlock Text="Distance" Width="100" Style="{StaticResource TextBlock}"/>
                    <TextBox Text="{Binding DistanceColumn, UpdateSourceTrigger=PropertyChanged}" Width="100" Style="{StaticResource TextBox}"/>
                </StackPanel>
            </Grid>
            
            
            
                <Button Command="{Binding WriteToSpreadsheet}" Content="Write" Margin="0 5 10 5" Width="75" VerticalAlignment="Center" />
                <TextBlock Foreground="Red"
                    Text="{Binding StatusMessage}" 
                    Visibility="{Binding ErrorMessageVisibility}" 
                    Style="{StaticResource TextBlock}"/>
                <TextBlock 
                    Text="{Binding StatusMessage}" 
                    Visibility="{Binding SuccessMessageVisibility}" 
                    Foreground="Green"
                    Style="{StaticResource TextBlock}"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
</Window>
